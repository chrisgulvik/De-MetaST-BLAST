#include <string>
using namespace std;

string complement(string);  //complements a string
char   comp2(char);
string flip(string);        //reverses the order of the string
string convert(string);     //converts to bitstring
int Sfind(string, string, int);  //returns location of string 1 in string 2 starting from the int param
